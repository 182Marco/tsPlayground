import * as c from './concepts';

console.log(`c.OrganizeInSingleObj: `, c.OrganizeInSingleObj(c.users));
console.log(`c.aggregateCars(c.cars): `, c.aggregateCars(c.cars));
console.log(`piramids: `, c.getAggregate(c.pyramids));
