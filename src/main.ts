import * as c from './concepts';

console.log(`users ---> `, c.getAggregateUsers(c.users));
console.log(`cars ---> `, c.getAggregateCars(c.cars));
console.log(`piramids ---> `, c.getAggregatePiramids(c.pyramids));
console.log(`rivers ---> `, c.getAggrgateRivers(c.rivers));
console.log(`cities ---> `, c.aggregateCities(c.cities));
